Flask==2.2.2
Werkzeug==3.1.3
jinja2  # for template rendering
pdflatex  # to convert LaTeX to PDF (you might need to install this system-wide too)
requests  # for making HTTP requests to the LaTeX service
